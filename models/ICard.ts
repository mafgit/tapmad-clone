
export default interface ICard {
  title: string;
  description?: string;
  image_url: string;
  start_date?: string;
  end_date?: string;
  date_time?: string;
  is_video?: boolean;
  is_live?: boolean
  id: string;
  long_description?: string
}

